function setup() {
  createCanvas (windowWidth, windowHeight); //this creates the window W/H to be the size of my browser windowWidth
  background (139, 151, 173); // grey/blue color background
}

function draw() {
}
